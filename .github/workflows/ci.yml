name: IPL Analytics CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test-and-evaluate:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Pytest Suite
      run: |
        pytest -v tests/

    - name: Run Data Quality Evaluation
      run: |
        python evaluation/evaluate_data_quality.py

    - name: Run Statistics Validation
      run: |
        python evaluation/evaluate_statistics.py

    - name: Run Dashboard Pipeline Evaluation
      run: |
        python evaluation/evaluate_dashboard.py
